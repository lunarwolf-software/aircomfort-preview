# No environment variables are required to run this project.
# All editable business information lives in src/data/business.ts.
#
# If you deploy to a host that requires an explicit base path instead of
# the default relative base ('./'), you can adjust `base` in vite.config.ts.
